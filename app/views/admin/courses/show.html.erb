<% provide :title, @course.name %>
<div class="col-md-12">
  <div id="content">
    <h1><%= @course.name %></h1>
    <li class="list-group-item list-group-item-info">
      <%= @course.description %>
    </li></br>
    <%= link_to 'Edit description', edit_admin_course_path(@course),
      remote: true, class: "btn btn-primary" %>
  </div>
</div>